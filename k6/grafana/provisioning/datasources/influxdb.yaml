apiVersion: 1

# Grafana 시작 시 자동으로 InfluxDB 데이터소스를 등록합니다
datasources:
  - name: InfluxDB
    type: influxdb
    uid: influxdb
    access: proxy
    url: http://influxdb:8086    # Docker 네트워크 내부 주소
    database: k6                 # k6가 결과를 저장하는 DB 이름
    isDefault: true
    editable: true
