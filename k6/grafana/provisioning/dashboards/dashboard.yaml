apiVersion: 1

# Grafana 시작 시 이 디렉터리의 JSON 파일을 대시보드로 자동 등록합니다
providers:
  - name: k6-dashboards
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards
